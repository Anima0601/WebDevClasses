
  /*  let n = parseInt(prompt("Enter the length of the array:"));
    let arr = [];
    for (let i = 0; i < n; i++) {
        let element = prompt(`Enter element ${i + 1}:`);
        arr.push(element); 
    }
    arr[n-1]=arr[0];
    for(i=1;i<n;i++){
        arr[i-1]=arr[i];
    }

    console.log("The new array you entered is:", arr);*/